{
    "command": "/opt/memcached_exporter/memcached_exporter --web.listen-address {{ 'api' | kolla_address(addr_context='url') }}:{{ prometheus_memcached_exporter_port }} --memcached.address {{ 'api' | kolla_address(addr_context='url') }}:{{ memcached_port }}",
    "config_files": [],
    "permissions": [
        {
            "path": "/var/log/kolla/prometheus",
            "owner": "prometheus:kolla",
            "recurse": true
        }
    ]
}
